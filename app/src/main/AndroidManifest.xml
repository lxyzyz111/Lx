<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.lxy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:anyDensity="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lxy"
        android:usesCleartextTraffic="true">
        <activity android:name=".fragment.ContainerActivity"></activity>
        <activity
            android:name=".BActivity"
            android:label="B"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="com.lxy.test" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AActivity"
            android:label="A"
            android:launchMode="singleTop" />
        <activity android:name=".LifeCycleActivity" />
        <activity
            android:name=".PopupActivity"
            android:label="Test" />
        <activity android:name=".CustomDialogActivity" />
        <activity android:name=".ProgressActivity" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".ToastActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".NewRecyclerActivity" />
        <activity android:name=".PubuliuActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".TextViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>